server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3354/geological?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: geological
    password: geological2024
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6427
      password: geological2024
      timeout: 6000ms

logging:
  level:
    com.example.geological: DEBUG
    org.hibernate.SQL: DEBUG
